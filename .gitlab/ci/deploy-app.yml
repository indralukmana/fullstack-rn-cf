deploy_app:
  stage: deploy
  interruptible: false
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  needs:
    - typecheck_app
    - test_e2e
    - build_app
  script:
    - pnpm dlx wrangler pages deploy apps/app/dist --project-name=rn-cf-web
