deploy_api:
  stage: deploy
  interruptible: false
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  needs:
    - typecheck_api
    - test_api
  script:
    - pnpm --filter @rn-cf/api deploy
