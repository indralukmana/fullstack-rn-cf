# Claude Instructions

Follow [`AGENTS.md`](AGENTS.md) as the canonical repository contract and read the nearest
scoped `AGENTS.md` before editing a workspace.

Do not infer permission to deploy, push, migrate remote data, change secrets or billing, or use
destructive Git. Keep each verified logical change small and reviewable.
